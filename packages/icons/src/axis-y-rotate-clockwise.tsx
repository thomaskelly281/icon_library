import { mdiAxisYRotateClockwise } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function AxisYRotateClockwise(props: IconComponentProps) {
  return <Icon path={mdiAxisYRotateClockwise} {...props} />;
}

export { mdiAxisYRotateClockwise as path };
