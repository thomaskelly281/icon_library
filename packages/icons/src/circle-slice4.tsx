import { mdiCircleSlice4 } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function CircleSlice4(props: IconComponentProps) {
  return <Icon path={mdiCircleSlice4} {...props} />;
}

export { mdiCircleSlice4 as path };
