import { mdiSeatLegroomExtra } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function SeatLegroomExtra(props: IconComponentProps) {
  return <Icon path={mdiSeatLegroomExtra} {...props} />;
}

export { mdiSeatLegroomExtra as path };
