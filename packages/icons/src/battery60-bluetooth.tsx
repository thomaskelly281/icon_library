import { mdiBattery60Bluetooth } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function Battery60Bluetooth(props: IconComponentProps) {
  return <Icon path={mdiBattery60Bluetooth} {...props} />;
}

export { mdiBattery60Bluetooth as path };
