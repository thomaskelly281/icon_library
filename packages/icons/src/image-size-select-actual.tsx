import { mdiImageSizeSelectActual } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function ImageSizeSelectActual(props: IconComponentProps) {
  return <Icon path={mdiImageSizeSelectActual} {...props} />;
}

export { mdiImageSizeSelectActual as path };
