import { mdiTableRowHeight } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function TableRowHeight(props: IconComponentProps) {
  return <Icon path={mdiTableRowHeight} {...props} />;
}

export { mdiTableRowHeight as path };
