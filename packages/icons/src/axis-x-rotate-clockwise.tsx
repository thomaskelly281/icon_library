import { mdiAxisXRotateClockwise } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function AxisXRotateClockwise(props: IconComponentProps) {
  return <Icon path={mdiAxisXRotateClockwise} {...props} />;
}

export { mdiAxisXRotateClockwise as path };
