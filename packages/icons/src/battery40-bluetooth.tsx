import { mdiBattery40Bluetooth } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function Battery40Bluetooth(props: IconComponentProps) {
  return <Icon path={mdiBattery40Bluetooth} {...props} />;
}

export { mdiBattery40Bluetooth as path };
