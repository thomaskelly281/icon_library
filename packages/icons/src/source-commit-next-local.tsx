import { mdiSourceCommitNextLocal } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function SourceCommitNextLocal(props: IconComponentProps) {
  return <Icon path={mdiSourceCommitNextLocal} {...props} />;
}

export { mdiSourceCommitNextLocal as path };
