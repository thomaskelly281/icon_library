import { mdiCheckboxIntermediateVariant } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function CheckboxIntermediateVariant(props: IconComponentProps) {
  return <Icon path={mdiCheckboxIntermediateVariant} {...props} />;
}

export { mdiCheckboxIntermediateVariant as path };
