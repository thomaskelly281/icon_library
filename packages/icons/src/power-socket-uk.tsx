import { mdiPowerSocketUk } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function PowerSocketUk(props: IconComponentProps) {
  return <Icon path={mdiPowerSocketUk} {...props} />;
}

export { mdiPowerSocketUk as path };
