import { mdiSeatLegroomNormal } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function SeatLegroomNormal(props: IconComponentProps) {
  return <Icon path={mdiSeatLegroomNormal} {...props} />;
}

export { mdiSeatLegroomNormal as path };
