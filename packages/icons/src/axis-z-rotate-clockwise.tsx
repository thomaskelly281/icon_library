import { mdiAxisZRotateClockwise } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function AxisZRotateClockwise(props: IconComponentProps) {
  return <Icon path={mdiAxisZRotateClockwise} {...props} />;
}

export { mdiAxisZRotateClockwise as path };
