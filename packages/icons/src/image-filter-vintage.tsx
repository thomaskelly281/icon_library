import { mdiImageFilterVintage } from "@mdi/js";
import { Icon, type IconComponentProps } from "./icon.js";

export function ImageFilterVintage(props: IconComponentProps) {
  return <Icon path={mdiImageFilterVintage} {...props} />;
}

export { mdiImageFilterVintage as path };
